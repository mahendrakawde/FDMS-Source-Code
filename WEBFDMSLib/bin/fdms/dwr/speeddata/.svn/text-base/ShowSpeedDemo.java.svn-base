package fdms.dwr.speeddata;

//import org.apache.log4j.Logger;

/**
 * ShowSpeedDataDemo - Interface to the front end for Demographic Speed Data.
 * This class will repopulate the name array with every key up from the front end.
 * It will also populate the rest of the data when the user tabs out of the name field.
 * Creation date: (9/15/2005 8:34 A)
 * @author: Kier Heyl
 */

//class ShowSpeedDemo {

    //private static Logger logger = Logger.getLogger(ShowSpeedDemo.class.getName());
    /*
    public ArrayList getNames (String category, HttpServletRequest request) {
        	
        HttpSession session = request.getSession();
        DbUserSession sessionUser = SessionHelpers.getUserSession(request);
    	
        SpeedDataManagerBean DemoData = new SpeedDataManagerBean();

    	ArrayList DemographicArray = DemoData.getDemoData (
    					sessionUser.getDataUrl(), 
                        sessionUser.getSqlUser(), 
                        sessionUser.getSqlPassword(),
						sessionUser.getRegion(),
						sessionUser.getLocationId(),
						category);
    	
    	return DemographicArray;
	}
	*/
//}