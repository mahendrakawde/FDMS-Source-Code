package fdms.ui.struts.form;

import javax.servlet.http.HttpServletRequest;

import org.apache.struts.action.ActionErrors;
import org.apache.struts.action.ActionMapping;


public class CheckListForm extends CheckListFormBase {

  /**
	 * 
	 */
	private static final long serialVersionUID = -1827821169923325683L;
public void reset(ActionMapping actionMapping, HttpServletRequest request) {
  /**@todo: Override this org.apache.struts.action.ActionForm method*/
  super.reset(actionMapping, request);
  this.setChecklistId(null);
  this.setChecked1(false);
  this.setChecked2(false);
  this.setChecked3(false);
  this.setChecked4(false);
  this.setChecked5(false);
  this.setChecked6(false);
  this.setChecked7(false);
  this.setChecked8(false);
  this.setChecked9(false);
  this.setChecked10(false);
  this.setChecked11(false);
  this.setChecked12(false);
  this.setChecked13(false);
  this.setChecked14(false);
  this.setChecked15(false);
  this.setChecked16(false);
  }
  public ActionErrors validate(ActionMapping actionMapping, HttpServletRequest request) {
  /**@todo: Override this org.apache.struts.action.ActionForm method*/
  return super.validate(actionMapping,  request);
  }
}
